{
  "name": "veilbound-phase-1",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mobile-first Phaser browser game scaffold for Veilbound. Phase 1 adds menu, save, hub, customization, options, gallery, and scene viewer skeletons using placeholder-safe assets only.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^6.3.1"
  }
}
